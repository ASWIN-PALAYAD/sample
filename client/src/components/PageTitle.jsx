import React from 'react'

const PageTitle = ({tilte}) => {
  return (
    <div>
        <div>
            <h1 className='text-xl' >{tilte}</h1>
        </div>

    </div>
  )
}

export default PageTitle